import React from "react";

const Main = ({ form, socialsChannels }) => {
  return (
    <main className="contact  page-padding-top">
      {form}
      {socialsChannels}
    </main>
  );
};

export default Main;
