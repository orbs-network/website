import React from "react";
import MainGrid from "../../shared/MainGrid";

function main({ cards }) {
  return (
    <div className="liquidity-hub-section-2 liquidity-hub-section">{cards}</div>
  );
}

export default main;
