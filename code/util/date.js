export const daysFromDate = (date) => {
  //   const start = moment(date, "DD/MM/YYYY");
  //   const end = moment();

  try {
    return 0;
  } catch (error) {
    return 0;
  }
};

export const formatBlogDate = (date) => {
  return 0;
};
